node_modules/
dist/
build/
*.egg-info/
__pycache__/
.pytest_cache/
.ipynb_checkpoints/
.venv/
*.venv/
# The built bundle in jbrowse_anywidget/static/ IS committed so that
# `pip install git+https://...` (and Colab) work without a JS toolchain.

# generated by scripts/run_examples.py: the specs, and the kernel-local files
# the notebooks wrote, which their tracks then read by byte range
scripts/screenshot_specs.json
scripts/captured/

# the package installs with pip; a uv lock is whatever a contributor ran locally
uv.lock

# tsc --noEmit still writes this via the inherited incremental config
tsconfig.tsbuildinfo
